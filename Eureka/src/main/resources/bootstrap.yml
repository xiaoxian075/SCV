spring:
  application:
    name: service-eureka
  cloud:
    config:
      profile: dev
      label: trunk
      uri: http://192.168.1.27:8868
#      discovery:
#        enabled: true #开启通过服务来访问Config Server的功能
#        service_id: service-config